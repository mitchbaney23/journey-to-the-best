import ProfileScreen from '../src/screens/ProfileScreen';

export default ProfileScreen;
//
// This file serves as the entry point for the Profile screen in the app.
// It imports the ProfileScreen component from the src/screens directory and exports it as the default export
